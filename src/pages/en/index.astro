---
import BaseLayout from "../../layouts/BaseLayout.astro";
import HeroSection from "../../components/sections/HeroSection.astro";
import DiagnosticSection from "../../components/sections/DiagnosticSection.astro";
import HowItWorksSection from "../../components/sections/HowItWorksSection.astro";
import BlogSection from "../../components/sections/BlogSection.astro";
import CtaSection from "../../components/sections/CtaSection.astro";
import type { Locale } from "../../config";

const locale: Locale = "en";
const siteUrl = "https://estratario.com";
const canonical = `${siteUrl}/en/`;
const title = "Estratario — Strategic Core for AI-Powered Marketing";
const description =
	"Centralize your clients' knowledge into a reusable Strategic Core. Eliminate duplicate work in your marketing strategy with AI.";

const alternates = [
	{ lang: "es", url: siteUrl },
];

const organizationSchema = {
	"@context": "https://schema.org",
	"@type": "Organization",
	name: "Estratario",
	url: siteUrl,
	logo: `${siteUrl}/icon.svg`,
	description,
	sameAs: [
		"https://www.linkedin.com/in/cesarcuitino",
	],
};

const websiteSchema = {
	"@context": "https://schema.org",
	"@type": "WebSite",
	name: "Estratario",
	url: `${siteUrl}/en/`,
	description,
	inLanguage: "en",
};
---

<BaseLayout
	locale={locale}
	title={title}
	description={description}
	canonical={canonical}
	alternates={alternates}
	jsonLd={[organizationSchema, websiteSchema]}
>
	<HeroSection locale={locale} />

	<DiagnosticSection locale={locale} />

	<HowItWorksSection locale={locale} />
	<BlogSection locale={locale} />
	<CtaSection locale={locale} />
</BaseLayout>

<script is:inline>
	(() => {
		if (window.matchMedia("(prefers-reduced-motion: reduce)").matches) {
			document.querySelectorAll(".scroll-fade").forEach((el) => {
				el.style.opacity = "1";
				el.style.transform = "none";
			});
			return;
		}
		const o = new IntersectionObserver((entries) => {
			for (const e of entries) {
				if (!e.isIntersecting) continue;
				const el = e.target;
				if (el.hasAttribute("data-step-id")) continue;
				el.animate([{ opacity: "0", transform: "translateY(20px)" }, { opacity: "1", transform: "translateY(0)" }], { duration: 600, easing: "ease-out", fill: "forwards" });
				o.unobserve(el);
			}
		}, { threshold: 0.15 });
		document.querySelectorAll(".scroll-fade:not([data-step-id])").forEach((el) => o.observe(el));
	})();
</script>
